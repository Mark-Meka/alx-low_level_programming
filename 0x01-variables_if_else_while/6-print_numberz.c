#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (i = '0'; i <= '0' + 9; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);

}
